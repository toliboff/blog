<article class="user">
    <img src="<%= user.photo %>" class="users-image">
    <div class="users-info">
      <%= link_to user_path(user.id) do %>
        <span class="username"><%= user.name %></span>
      <% end %>
      <div class="user-posts">Number of posts: <%= user.posts_counter ? user.posts_counter : 0 %></div>
    </div>
</article>