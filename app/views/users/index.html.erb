<h1>Root users</h1>
<p>Here is a list of users</p>
<ul>
  <% @users.each do |user| %>
   <%= link_to user_path(user.id) do %>
     <%= render partial: 'user', locals: {user: user} %>
     <% end %>
  <% end %>
</ul>
